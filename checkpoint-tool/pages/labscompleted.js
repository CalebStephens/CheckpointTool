import LabsCompleted from "@/components/LabsCompleted";

export default function LabsCompleted() {
  return (
    <main
      className={`flex min-h-screen flex-col items-center p-24 ${inter.className}`}
    >
      <LabsCompleted />
    </main>
  );
}
